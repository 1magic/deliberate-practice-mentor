# CHANGELOG · 刻意练习教练（deliberate-practice-mentor）

> 记录每个发布版本的用户可见变更，提交 SkillHub 时可直接作为「变更说明」使用。

---

## v1.4.2 · 2026-09-10

**Token 瘦身**——SKILL.md 从 28.8KB 压到 16.8KB（**-42%**），每场会话的固定注入成本直接下降。**规则一条不丢**，只是「故事挪窝、重复合并、低频外置」。

**背景**：09-01 以来连续 5 次「只加规则、不减内容」，SKILL.md 累积翻倍（14.2KB → 28.8KB）；而 SKILL.md 每次会话开头全量注入，体积直接等于每轮固定 token 成本。

- **教训叙事外移**：各纪律小节原本内联 E-COACH 完整故事（教训链、活例细节、猜错成本），改为正文一句话锚点 + 指向错误账本
- **跨文件重复消重（7 处）**：SKILL.md 与 references 内容重叠——制图要点（已在 output-templates「通用制图规范」）、错误指正协议 / 找茬纪律 / 维度评分（已在 method-library）、基线评估 / 平台期 / 练习节奏（已在 method-library）→ SKILL.md 只留一句 + 指针
- **重复表述消重**：「≠掌握」家族（讲解≠掌握 / 复述≠掌握 / 结果对≠理解对 / 猜对≠掌握）原散落 5 处 → 合并到「掌握判定」单处定义；「概念引用纪律」与「概念引用扫描」两节合并为一节
- **低频分支下沉（按触发频率定档）**：教程关 / Boss 综合关 / 周月双轨回顾 / 计划漂移 / goal 粒度校准 → `teaching-discipline.md`；平台期检测 / 练习节奏 / 基线评估 / 找茬纪律 / 维度评分 → `method-library.md`（原已在，SKILL.md 改指针）；工作区初始化 → `workspace-templates.md`；判读材料（仅「出判读题」时适用）→ 整体下沉
- **高频纪律保留核心**：微结算（每场每步触发）、单轮投喂（每条消息触发）频率高 → 只下沉细则，可执行核心留在 SKILL.md——SKILL.md 每次会话注入一次，不写则执行那一刻不会遵守
- **正文压缩**：交互命令表格 → 紧凑列表；记忆分层表去「含义」列；工作循环 5 步各压到 1 行；更新日志压到 1 行
- **新增「技能维护纪律（防膨胀）」**：与原「修改授权门」合并为一节——先查重（跨文件双写是主要膨胀源）/ 正文只写规则、故事进错误账本 / 按频率归位 / SKILL.md 超 ~18KB 触发瘦身体检

**结果**：SKILL.md 251 行 → 179 行，20 个章节全部保留；`teaching-discipline.md` 承接下沉细则（按需加载）。

兼容：纯重构，方法论与判定规则零变更；旧项目零迁移。references 按需加载，日常练习零上下文增量。

---

## v1.4.1 · 2026-09-09

会话末总结图输出模板落地——把「图表化总结」从"画法语义描述"升级为"可直接填数的骨架"，稳定跨会话出图质量、省去每场从零设计 SVG 的 token 与翻车成本。

- **`references/output-templates.md`（新文件 · 按需加载）**：三图骨架——战报总览图（三块式：热力图变化 + DoD 达成 + 错误入账 + 状态条）/ 机制链路图（横向箭头流：环节=主标+证据副题，底部实证输出）/ 里程碑进度图（阶段进度条：闭环绿·进行蓝·待开始灰）；每份含选图规则 + 填写对照 + 已跑通参考 SVG + 防翻车清单（文字溢出自检公式、深色主题反转、emoji 禁令）
- **数据溯源硬约束**：图内数字必须来自本场蒸馏产物（goal-brief DoD / 热力图 / error-log / session-brief），禁止编造——防空壳图
- **SKILL.md**：「图表化总结」节挂指针（出图先取骨架、填数代替从零设计）；Resources 补条目；底部更新日志压缩为最近 1 版（约定落实，全量见本文件）
- **workspace-templates.md**：补「基线评估」文件契约小节（heatmap/基线评估.md；用法说明原在 method-library，两处呼应不重复）
- **README / SKILL_DESCRIPTION**：目录结构、版本历史、版本字段同步

兼容：纯 additive；references 按需加载（仅汇报步出图前读），日常练习零上下文增量；旧项目零迁移。

---

## v1.4.0 · 2026-09-09

游戏化（动机与结构工程）成套落地——借鉴游戏设计理论（SDT 自我决定论 / 正反馈频率 / 失败设计 / 关卡结构 / 好奇驱动），补齐 dp 在「动机工程 + 结构工程」的空洞，不碰既有判定纪律（DoD/BKT/IRT/FSRS 原文不动）。

- **步骤微结算（节拍反馈）**：每完成一步即时给 ≤1 句证据确认（15-25 分钟一次小胜利），补 45-90 分钟只 1 次大评估的频率空洞；双层语义——微结算=过程放行，不构成 🟢 掌握证据；反模式：无证据夸赞禁用（防反馈通胀）
- **错误=情报（失败翻转）**：goal-brief「障碍预判」升级「陷阱预告」（障碍 → 应对 → 卡住后拿到什么）；错误日志补「情报入账」语义；四段式加引导语（踩坑=拿免疫）——账本结构与防再犯纪律不变（同坑 ≥2 次仍升级规则区）
- **难度即时旋钮（双旋钮）**：每场反馈步问「挑战度 1-5 + 上/下/保持」当场微调（游戏逐关调难度）；与 IRT 趋势校准并存，冲突时以趋势为准、微调负责中场
- **教程关 + Boss 综合关（关卡结构两端）**：新主题默认首 1-2 场 tutorial goal（跑通系统最小闭环，已毕业 ≥1 主题可跳过）；每课末 / 每 3-4 个常规 goal 后插 1 个 boss goal（无新知识，组合前置技能，挂粒度校准检查点后）；goal-brief 新增「goal 类型」字段承载（normal/tutorial/boss）
- **知识地图待解锁区**：第四区块灰显未来 1-2 课概念名（只给名称不展开，制造期待）；与「延后主题清单」双轨（前者学习者可见，后者教练内部）；受点到为止约束
- **自主感（SDT）**：goal-brief 可选「练习路径 A/B」——同 DoD 下 2 条路径任选，只给「怎么练」选择权
- **红线**：全程无积分 / 徽章兑换 / 排行榜 / 随机奖励——overjustification 效应会侵蚀「掌握感」内在动机；战报图与热力图保持「记录里程碑」语义

兼容：纯 additive；goal 类型默认 normal / A-B 路径与待解锁区空着不填即可，旧项目零迁移。SKILL_DESCRIPTION / README 已同步 v1.4.0。

---

## v1.3.6 · 2026-09-09

难度校准 + BKT 证据计数——给「能力二 · 目标拆解与掌握判定」装上可校准、有依据的判定规则。

- **难度校准（成功率窗口 · 借鉴 IRT/CAT）**：拆新目标前查最近 3-5 场评估——连续 2-3 次 ✅+（超预期）→ i+1 偏低，难度上调；连续 2 次 ⚠️（未达标）→ 偏高，下调；以 ✅ 为主 → 保持。目标难度 = IRT 答对率 60-85% 窗口的 dp 版
- **BKT 证据计数（掌握判定）**：🟢 需 ≥2 次不同场景独立验证通过；🟡 = 仅 1 次或证据不足；🔴 = 无验证或失败——显式建模猜测 P(G)/失误 P(S)，单次答对不足为凭
- goal-brief「结果」回填「难度体感：太难/刚好/太简单」，供校准累积数据

兼容：纯 additive；旧 🟢🟡🔴 记录零迁移。

---

## v1.3.5 · 2026-09-09

间隔复习四档评级（借鉴 FSRS）——「轻松记住」与「勉强记住」的记忆强度不同，不该按同一序列递增。

- 档位序列 1d→3d→7d→14d→30d→已巩固 不变，按四档自适应：忘了→重置 1d ｜ 勉强→保持档 ｜ 正常→升一档 ｜ 轻松→跳两档
- 热力图回顾记录标注评级，作为 next_review 依据

兼容：纯 additive；旧记录（✅/❌）视为「正常/忘了」，无需回填；只作用于回顾环节。

---

## v1.3.4 · 2026-09-09

错误规则化——把错误账本从「事后复盘」升级为「开工前预警」：复发错误（≥2 次）补 `trigger`（触发边界）+ `validation`（自查项），经验从「记过」变「防再犯」。

- 错误账本分两类：一次性错误（只留四段式）/ 规则区（复发或模式性错误）
- 三个消费点：喂 goal-brief「障碍预判」/ 练习前预警（按 trigger 查命中）/ 挂概念卡 links

兼容：纯 additive；旧记录视为一次性错误，无需回填。

---

## v1.3.3 · 2026-09-09

记忆分层（L0-L3）+ 分层读取（借鉴分层蒸馏模型）——教练醒来先读画像/场景层引导，按需回退事件/原始层，取代一次性全读，降低每次醒来的 token 消耗。

- **记忆分层**：L3 画像（热力图+知识地图+基线）/ L2 场景（goal-log+概念卡）/ L1 事件（错误账本+反馈日志）/ L0 原始（会话记录）
- **分层读取**：「准备」步先读 L3/L2 快速引导，按需回退 L1/L0 限条数（反全量扫描）
- **蒸馏显式化**：工作循环第 4 步「蒸馏」= session(L0) → L1 事件 + L2 场景 + L3 画像

兼容：纯 additive；文件结构不变，仅读取顺序变化，旧项目零迁移。

---

## v1.3.2 · 2026-09-09

概念卡机制（借鉴 OKF 字段语义，不自称 OKF）——把「有故事」的知识点/心智模型/错误按需外挂为独立卡片，**掌握度由验证事件推导、不手填**（治判定虚高）。采取「借字段语义、不借文件粒度」的克制策略。

- **索引表 + 按需外挂**：仅踩坑/回炉/需证据链/心智模型建档 `concepts/<slug>.md`；无故事概念留在索引表不产生文件
- **字段契约**：`slug`（键控引用）/ `type` / `status`（已学·钩子·延后）/ `resource`（溯源 1:1）/ `verified`（验证事件）/ `next_review`（绝对日期）/ `links`（互链，断链容忍=钩子载体）
- **掌握度推导**：🟢🟡🔴 由 verified 事件推导（无 verified ⇒ 🔴），教练只记事件不填结论
- **引用纪律**：跨文件引用一律 slug 键控，不用位置/行号

兼容：纯 additive；无卡知识点维持手填过渡。

---

## v1.3.1 · 2026-09-07

判读材料供给纪律（出题侧）——既有「答案后置」只管判卷侧，缺出题侧对称纪律：**判读题材料必须自足，「猜对≠掌握」**。

- **材料自足**：判读题所涉枚举/术语语义必须材料内含（概念锚一句话/给源码或文档路径自取查证），禁止把「没见过语义」当判读题
- **考推断有边界**：可考推断但材料须够推，纯猜谜无意义
- **推对 ≠ 猜对**：学习者标注「猜测」→ 判卷区分推断链成立 vs 蒙对，后者不算掌握证据，补材料重判
- **「猜对≠掌握」并入 ≠掌握 家族**（讲解/复述/结果对 同理）

兼容：纯新增规则，对既有项目无迁移成本。

---

## v1.3.0 · 2026-09-03

课编号制 + 场次粒度——「周」与「目标」双编号在按日高频练习下冗余（一周拆一 goal 时两号同构），且「周」是假承诺（实际按天练习）。课程单元统一 **`L-00X` 单编号**，周编号退场；时间粒度从「周」改「场」（1 次会话 45-90 分钟）。

- 长期计划层级：阶段 → 课列表（每课标建议场数）→ 每日任务清单
- goal-brief 标「建议 N 场」；session-brief 标场号；节奏「周 4-5 次」→「日 1-2 场/周 3-5 场」
- 阈值场次化：平台期「连续 2 周」→「连续 ≥4 场」；漂移「落后 >1 周」→「落后 >4 场」
- 双轨兼容：有课时体系项目保留讲义锚点编号；无课时项目存量全量迁移为 L-00X
- 模板同步：goal-brief/session-brief/feedback 加课号与场次标注

兼容：周/月双轨回顾保留（与课编号解耦）；错误账本 E-XXX 编号不变。

---

## v1.2.8 · 2026-09-02

单轮投喂纪律——技能只有「步骤顺序」纵向纪律，缺「单条消息信息密度」横向纪律（步骤稳、投喂爆）。学习者修正约束：**不限制题量，限制主题数**。

- **每轮 = 1 个验证主题**：问题可多个，主题唯一；补全/对比/checklist 属同主题
- **跨主题单发**：新概念图、下一任务、他主题裁决单独一轮
- **答案后置**：预跑/标准答案只作判卷对照，不先投喂；演示教学先试再演示
- **带宽调节**：能量 <5/10 或分心 → 最小块；开关在学习者（「多给」放开、「慢」收紧），教练不猜带宽

兼容：无破坏性变更；只约束教练单条消息形态。

---

## v1.2.7 · 2026-09-01

token 消耗优化版——三项瘦身，行为不变。

- **S1 · 更新日志瘦身**：SKILL.md 底部日志只留最近 1 版 + 一行摘要，更早版本移入本文件（日志是每次加载的固定死重）
- **S2 · 范围覆盖矩阵「来源纪律」**：知识点只来自「已有清单 + 标题一次性录入」，禁止为填矩阵全量读讲义/PDF/WebFetch
- **S3 · 授权门方案限长 ≤15 行**：给学习者确认的改动方案要点式列出

兼容：无破坏性变更，仅省 token；历史版本完整保留于本文件。

---

## v1.2.6 · 2026-09-01

范围覆盖矩阵——拆解 goal 时强制盘点名义范围内全部知识点并标注去向，堵住「名义范围大、DoD 只覆盖主攻环」的偏科漏学。

- 三态标注：① 本 goal 覆盖（挂 DoD 验收）② 延后（注明去向，不许静默消失）③ 仅铺垫（钩子，不入考核）
- 拆解自检：goal 标题范围 = DoD 覆盖范围，不一致就收缩标题或补 DoD
- goal-brief 模板同步加「范围覆盖矩阵（强制）」字段

兼容：无破坏性变更；新拆解强制填写，既有 goal-brief 不受影响。

---

## v1.2.5 · 2026-09-01

技能修改授权门——学习者硬规则：**任何对技能文件的修改必须先确认**（先给方案、点头后动手）。

- 技能文件（SKILL.md/references/模板）修改一律先呈现方案、经确认
- 记忆文件（MEMORY.md/工作日志）不在此限
- 「修改」「更新技能」指令 = 授权入口，仍须先呈现方案

兼容：无破坏性变更；后续所有技能迭代均需确认后执行。

---

## v1.2.4 · 2026-09-01

课时确认条件化（修正 v1.2.3 过度泛化）——不是每个项目都有课时和视频。

- 「确认当前课时」从无条件准备步骤降级为条件步骤：仅讲义/视频驱动项目执行，代码/文档驱动项目跳过
- 「练习-讲义课时对应」小节标注「仅讲义/视频驱动项目 · 条件适用」

兼容：无破坏性变更；无课时项目减少一步死步骤。

---

## v1.2.3 · 2026-09-01

练习-讲义课时对应——把「课时坐标」制度化进准备流程（学习节奏 = 看视频 → 练习闭环）。

- 计划中每个 goal 标注课时编号 `课时 N-x`（与讲义视频编号一致）
- 每次练习开始先确认「当前课时」，未看对应讲义先补看再练
- 无讲义对应的延伸练习 → 标注「无课时」，走代码/文档驱动
- 交互命令更新：练习前确认 = 项目归属 + 当前课时两步

兼容：无破坏性变更；无讲义项目不强制课时确认。

---

## v1.2.2 · 2026-09-01

多项目空间消歧——学习者同时维护两个并行练习项目，两次「猜默认项目」都猜错方向（E-COACH-002/003）。确立：**各自空间练各自项目、不默认任何一个**。

- 指令显式点名项目 → 直接进对应工作区；未点名（「继续下一课」「开始练习」）→ 不猜测，第一步先确认「哪个项目？」
- 目录归属/最近会话仅作辅助线索，不做默认
- 跨项目知识点联系降级为可选

兼容：目录与工作区契约不变；单项目学习者不触发确认。

---

## v1.2.1 · 2026-09-01

课程设计源头治理——把两次元认知纠出的盲区制度化：**串联心智没建模 = 不教**、**新概念被当已知词引用**；同时修正措辞：引入新概念（铺路/埋钩子）允许且有价值，红线是「不得以考核答案方式出现」。

- **心智模型清单（能力一）**：知识地图除名词知识点外，每阶段显式列出 ≥2 个「动词性心智模型/流程」；名词 = 是什么，模型 = 怎么串；**不建模 = 不教**；同样进热力图
- **概念引用扫描（能力二）**：goal-brief 新增「概念引用清单」= 引用概念 + 热力图状态 + 用途三态（① 已学直接用 ② 新概念·铺路：钩子+够用抽象，不入考核 ③ 新知识点：教学+独立验证）；🔴 概念允许以 ② 铺垫，但出现在 DoD/考题必须为 ③——区分「引入」与「考核」
- **链路验证强制项（能力二）**：每 goal ≥1 个 DoD 为「走通/重述 ≥3 环链路」——名词验证可被「讲过」蒙混，链路验证把串联心智显式化
- **新概念引用纪律**：首次引用未学过的串联心智须标注「新概念/钩子」+ 够用抽象；「学过组成部分」≠「会串联」，「出现过」≠「教过」

兼容：目录与工作区契约不变，存量记录无缝续用。

---

## v1.1.0 · 2026-08-31

练习质量门禁 + 长期节奏校准——把早期实践教训制度化：**DoD 判定虚高**（讲解≠掌握）与**目标粒度错位**，并新增练习收尾的图表化总结。

- **goal 粒度校准机制（能力四）**：触发 = 周回顾顺带 1 次 / 每完成 3-4 个 goal 必问 / 主动喊「调粒度」；判断信号（频繁超时或 DoD 靠赶完 = 偏大→拆细；30 分钟内且单薄 = 偏小→合并；45-90 分钟从容 = 刚好）；结论记 feedback-log + 更新 plan
- **DoD 判定强制规则（能力二）**：通过必须基于独立产出证据（换场景题/独立复述/独立演示）；讲解、点头、结果正确、「全懂」均 ≠ 掌握；判定虚高 → 回炉 + 降级
- **验证方式纪律**：每验证步骤写明验证方式并一起打勾，禁止「讲解 + 点头/复述」充当
- **图表化总结**：默认机制链路图/战报总览图/里程碑进度图（图讲结构、文字讲结论）
- **新增交互命令**：「调粒度」「粒度」；周检/月检顺带校准
- frontmatter：新增 `agent_created: true`；移除 `allowed-tools`

兼容：目录与 references/ 未改动；工作区契约不变。

---

## v1.0.0 · 2026-08-28

首版发布：四大能力（长期计划设计 / i+1 目标拆解 / 练习方法设计+错误指正 / 反馈与学习集合调整）+ 12 种练习方法 + 追踪体系（掌握热力图/基线评估/错误账本/每日打卡）+ 科学机制（Ebbinghaus 间隔复习/平台期检测/周月双轨回顾）。
